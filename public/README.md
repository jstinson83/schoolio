Unused. `firebase.json`'s catch-all rewrite sends every request straight to
the Cloud Run service, so nothing here is ever served — this directory only
exists because Firebase Hosting requires a `public` dir to be present.
